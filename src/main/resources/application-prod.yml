spring:
  datasource:
    url: jdbc:postgresql://db:5432/hospitaldb
    username: postgres
    password: ${DB_PASSWORD}

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

server:
  servlet:
    context-path: /api/v1